import { exampleUsage } from './client';

// Ejecutar la función de ejemplo al iniciar la aplicación
exampleUsage().catch((error) => {
  console.error("Error en la ejecución del ejemplo:", error);
});